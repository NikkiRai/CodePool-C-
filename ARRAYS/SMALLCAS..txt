*Count the number of digits,capital letters,small case vowels and 
  blanks in an array:

#include<iostream.h>
#include<conio.h>
#include<stdio.h>
void main()
{ clrscr();
char a[30];
int n,c,d,v,b;
c=d=v=b=0;
cout<<"Enter String (25)";
gets(a);int i=0;
while(a[i]!='\0')
 {
 if (a[i]==' ') b++;
else if (a[i]>='A' && a[i]<='Z') c++;
else if (a[i]>='0' && a[i]<='9') d++;
else if(a[i]=='a' || a[i]=='e' || a[i]=='i' || a[i]=='o' || a[i]=='u') v++;
i++;
}
cout<<'\n';
cout<<"Digits="<<d<<'\n';
cout<<"Capital Letters="<<c<<'\n';
cout<<"Small Case Vowels="<<v<<'\n';
cout<<"Blanks="<<b<<'\n';
getch();
}

