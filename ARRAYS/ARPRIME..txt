* PRIME NUMBERS:

# include <iostream.h>
# include <conio.h>
void main()
{ clrscr();
int a[30];
int n, num, c=0,j;
cout << "Enter String Length : ";
cin >> n;
cout << "\nEnter any " <<n<<" integers\n";
for (int i=0;i<n;i++)

cin >> a[i];

cout << "Prime numbers are ";
for (i=0;i<n;i++)
{j=2;
while (j<=(a[i]/2) && (a[i]%j)!=0)
j++;
if ((a[i]%j)!=0 || a[i]==2)
{ cout << a[i] << ' ';
c++; }
}

cout << "\nNumber of Prime Numbers = " << c;
getch(); }