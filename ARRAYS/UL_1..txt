APPLICATIONS:

=> Receive an array of n characters. Also receive a character. Convert each occurance of
a given character in opposite case:

#include<iostream.h>
#include<conio.h>
#include<stdio.h>
#include<ctype.h>
void main()
{
clrscr();
char a[30],c;
int i=0;

cout<<"Enter the string:"<<'\n';
gets(a);
cout<<"\nEnter the character to change its case:\n";
cin>>c;
c=tolower(c);

while(a[i]!='\0')
{
  if(a[i]==c)
   a[i]=toupper(a[i]);

 else if (a[i]==toupper(c))
 a[i]=tolower(a[i]);

 i++;
}
 cout<<"New string"<<'\n'<<a;


 getch();
  }